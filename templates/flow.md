---
title: "Flow: <name> — <one-line scenario>"
summary: One line — rendered verbatim in the generated index.md.
category: flows
kind: explanation
audience: [dev]
read_when: "investigating <this runtime path> or touching the code it traverses"
# --- flow-meta (the diagram + tables below the marker are GENERATED from this;
#     never hand-author Mermaid). Schema + the verification ladder: references/flow.md.
flow_schema: flow-meta/v1
flow_scenario: "<one named scenario — a request/job/sync/tool path, including error paths>"
flow_trigger_kind: command        # http | command | cron | queue | event | call
flow_trigger_anchor: <path/to/entrypoint>
flow_asserts_complete: false      # true only if a user-space set-equality extractor is registered
flow_steps:
  - id: <step-id>
    actor: <who runs it>
    action: "<what happens here>"
    anchor_path: <repo/relative/file>
    anchor_sha: WRITTEN-BY-wiki-stamp
    anchor_match: "<a string that occurs in that file>"
flow_edges:
  # A `verified` edge cites the CALL SITE in the FROM-step's own code and names
  # the callee. If the hop can't be cited that way (dynamic dispatch, sequence,
  # cross-file indirection), mark it `inferred` — an honest authorial claim.
  - from: <step-id>
    to: <step-id>
    kind: call                    # call | async | queue | http | db | event
    evidence: verified            # verified | inferred
    call_anchor_path: <same file as the from-step's anchor_path>
    call_anchor_lines: <n or n-m, narrow to the call site (≤ 40 lines)>
    call_anchor_sha: WRITTEN-BY-wiki-stamp
    call_match: "<a string in that span>"
    callee_token: "<a token naming the callee, present in the span>"
    # async/queue/event ALSO need a handler anchor:
    # handler_path / handler_lines / handler_sha / handler_match
flow_branches:
  - at: <step-id>
    to: <step-id>
    condition: "<when this fork is taken>"
    kind: error                   # error | guard | alt | normal
    cite_path: <repo/relative/file>
    cite_lines: <n or n-m>
    cite_sha: WRITTEN-BY-wiki-stamp
    cite_match: "<a string at the fork>"
covers:
  - path: <repo/relative/file>
    sha: WRITTEN-BY-wiki-stamp
generated_at_commit: WRITTEN-BY-wiki-stamp
last_refreshed: WRITTEN-BY-wiki-stamp
related: []
---

# Flow: <name> — <one-line scenario>

> One named scenario, schematic not exhaustive (arc42 §6). The diagram + tables
> below the marker are generated by `wiki-flow-render.mjs` from the `flow_*`
> frontmatter — run it after every edit; never hand-edit inside the markers.

## Scenario

<Two or three sentences: the trigger, the happy path, and the branches that
divert it. Keep it schematic — collapse pure statement-sequence into one step.>

<!-- FLOW-RENDER:START — generated from flow-meta; do not hand-edit (run wiki-flow-render.mjs) -->
<!-- FLOW-RENDER:END -->

## Notes

<Why edges are verified vs inferred; what completeness this page does (not)
claim; anything a maintainer re-verifying after a code change should know.>

<!--
Authoring loop:
1. Write flow_* (steps with anchors, edges with directional call-site citations
   + per-edge evidence, branches including error paths).
2. node .repolore/scripts/wiki-flow-render.mjs <this-page>   # fill the region + sidecar
3. node .repolore/scripts/wiki-stamp.mjs <this-page>         # write the blob SHAs
4. node .repolore/scripts/wiki-flow-check.mjs <this-page>    # gate the tier
Tiers (computed, never committed): structural → anchored → edge-cited →
branch-audited → set-validated (user-space extractor, opt-in). See references/flow.md.
-->
